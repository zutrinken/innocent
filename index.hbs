{{!< default}}

<header id="main-header">
	<nav id="main-nav">
		<span id="rss-link"><a class="subscribe" href="{{@blog.url}}/rss/"><i class="icon-rss"></i> RSS</a></span>
	</nav>
	<a class="site-title" class="js-ajax-link js-show-index" href="{{@blog.url}}">
		{{#if @blog.logo}}
			<span class="site-logo"><img src="{{@blog.logo}}" alt="Logo" /></span>
		{{/if}}
			<h1 class="site-name">{{@blog.title}}</h1>
		{{#if @blog.description}}
			<span class="site-description">{{@blog.description}}</span>
		{{/if}}
	</a>
	{{#if @blog.cover}}
		<div class="site-cover" style="background-image: url({{@blog.cover}})"></div>
	{{/if}}
</header>

<div id="post-index" class="container">

	<main class="content" role="main">
	
		{{> "loop"}}
	
	</main>

</div>